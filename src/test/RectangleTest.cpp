#include <gtest/gtest.h>
#include "../Rectangle.h"

using namespace spatialindex;
/*
TEST(RectangleTest, Intersects) {
    Rectangle r1(0, 0, 10, 10);
    Rectangle r2(5, 5, 15, 15);
    Rectangle r3(20, 20, 30, 30);

    EXPECT_TRUE(Rectangle::intersects(r1));
    EXPECT_FALSE(Rectangle::intersects(r1));
}

TEST(RectangleTest, Contains) {
    Rectangle r(0, 0, 10, 10);
    EXPECT_TRUE(Rectangle::contains(r));
    EXPECT_FALSE(Rectangle::contains(r));
}

TEST(RectangleTest, DistanceSq) {
    // Rectangle r(0, 0, 10, 10);
    EXPECT_EQ(Rectangle::distanceSq(0, 0, 10, 10, 5, 5), 0.0); // Point inside rectangle
    EXPECT_EQ(Rectangle::distanceSq(0, 0, 10, 10, 15, 15), 50.0); // Point outside rectangle
}
*/